extends layout
block content
	h1= title
	if errors
		p= errors
	form(method="POST", action="")
		label(for="name")
			span Name :
			input(type="text", name="name")
		label(for="password")
			span Password :
			input(type="password", name="password")
		button Submit

